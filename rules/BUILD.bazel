load("@platforms//host:constraints.bzl", "HOST_CONSTRAINTS")

config_setting(
    name = "multi_host_setting",
    constraint_values = HOST_CONSTRAINTS,
    flag_values = {"//:multi_flag": "True"},
)
