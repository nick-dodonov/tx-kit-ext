<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tx"
    android:versionCode="1"
    android:versionName="1.0" >

    <!-- TODO: make template based because "uses-permissions attributes are never exported" https://bazel.build/versions/6.2.0/reference/be/android#android_library.exports_manifest -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- TODO: allow HTTP in debug from deps:
    res/xml/network_security_config.xml:
        <?xml version="1.0" encoding="utf-8"?>
        <network-security-config>
            <base-config cleartextTrafficPermitted="true" />
        </network-security-config>
    <application
        android:networkSecurityConfig="@xml/network_security_config"
        ... >
    -->

    <uses-sdk
        android:minSdkVersion="24"
        android:targetSdkVersion="35" />

    <application
        android:label="Bazel App"
        android:hasCode="true"
        >
        <activity
            android:name="tx.DroidActivity"
            android:exported="true" >

            <meta-data
                android:name="android.app.lib_name"
                android:value="${native_lib_name}" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
