load("@rules_cc//cc:cc_library.bzl", "cc_library")

exports_files([
    "AndroidManifest.xml.template",
])

# Bridge library: android_main() -> main(0, nullptr) for reusing code with main() entry point.
cc_library(
    name = "droid_main",
    srcs = [
        "android_main.cc",
        "redirect_stdout.cc",
    ],
    hdrs = ["redirect_stdout.h"],
    deps = ["@androidndk//:native_app_glue"],
    visibility = ["//visibility:public"],
)
