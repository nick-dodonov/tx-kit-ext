load("@rules_cc//cc:cc_library.bzl", "cc_library")

exports_files([
    "AndroidManifest.xml.template",
])

# Standalone cc_library for droid bridge - compiles in this package so native_app_glue include works.
cc_library(
    name = "droid_main",
    srcs = ["droid_main.cc"],
    deps = ["@androidndk//:native_app_glue"],
    visibility = ["//visibility:public"],
)
