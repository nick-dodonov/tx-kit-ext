load("@rules_python//python:defs.bzl", "py_binary")

py_binary(
    name = "setup-user-lldbinit",
    srcs = ["setup-user-lldbinit.py"],
    data = [
        "default.user.lldbinit",
    ],
    deps = [
        "@pypi//termcolor",
    ],
)
